# sii-webdriverio-mocha

# requirements
- you must have installed chrome browser
- you must have installed node.js

# installation 
- in repository root directory run command "npm install"

# running tests
- in repository root directory run command "npm run wdio"